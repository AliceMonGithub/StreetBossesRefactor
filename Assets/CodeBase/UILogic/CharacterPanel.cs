using CodeBase;
using UnityEngine;

public class CharacterPanel : MonoBehaviour
{
    //[SerializeField] private CharacterPlacing _characterPlacing;
    //[SerializeField] private CharacterIcon _prefab;
    //[SerializeField] private Transform _panelParent;

    //[Space] [SerializeField] private PlayerStats _playerStats;

    //private void Start()
    //{
    //    Refresh();
    //}

    //private void Refresh()
    //{
    //    foreach (var character in _playerStats.Characters)
    //        Instantiate(_prefab, _panelParent).Initialize(character, _characterPlacing);
    //}
}