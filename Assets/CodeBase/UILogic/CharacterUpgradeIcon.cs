using TMPro;
using UnityEngine;
using UnityEngine.UI;

namespace CodeBase.UILogic
{
    public class CharacterUpgradeIcon : MonoBehaviour
    {
        //[SerializeField] private TMP_Text _levelText;
        //[SerializeField] private Image _heroImage;

        //private CharactersUpgradeMenu _charactersMenu;
        //private HeroUpgradeMenu _upgradeMenu;
        //private Character _character;
        
        //public void Select()
        //{
        //    _upgradeMenu.Show(_character, _charactersMenu);
        //}

        //public void Initialize(Character hero, HeroUpgradeMenu upgradeMenu, CharactersUpgradeMenu charactersUpgradeMenu)
        //{
        //    _levelText.text = (hero.Level + 1) + " / " + (hero.Upgrades.Length + 1);
        //    _heroImage.sprite = hero.Image;

        //    _charactersMenu = charactersUpgradeMenu;
        //    _upgradeMenu = upgradeMenu;
        //    _character = hero;
        //}
    }
}